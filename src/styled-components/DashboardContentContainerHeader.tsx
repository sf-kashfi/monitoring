import { styled, Grid } from "@mui/material";
export const DashboardContentContainerHeader = styled(Grid)(({ theme }) => ({
  background: theme.palette.primary.main,
  padding: ".7rem",
  marginBottom: ".4rem",
}));
